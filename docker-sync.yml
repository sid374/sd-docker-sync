version: "2"

options:
  verbose: true 
syncs:
  ubuntu-51-sync: # tip: add -sync and you keep consistent names as a convention
    src: '../../..'
    sync_excludes: ['ignored_folder', '.ignored_dot_folder']
    sync_userid: '502'
